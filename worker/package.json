{
  "name": "mcp-hub-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for MCP Hub - A registry and gateway for MCP servers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cloudflare-worker",
    "registry",
    "gateway",
    "api"
  ],
  "author": "MCP Hub Team",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "@typescript-eslint/eslint-plugin": "^6.7.0",
    "@typescript-eslint/parser": "^6.7.0",
    "eslint": "^8.50.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "vitest-environment-miniflare": "^2.14.4",
    "wrangler": "^3.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
